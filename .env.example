# Leave empty to use content/zh. Set an explicit path only for an external source folder.
SOURCE_MD_DIR=
APP_ENV=development
# Set to 1 only when running behind a trusted reverse proxy.
TRUST_PROXY=0
