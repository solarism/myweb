services:
  website:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file: .env
    environment:
      SOURCE_MD_DIR: /app/content/zh
    volumes:
      - ./content:/app/content:ro
    restart: unless-stopped
